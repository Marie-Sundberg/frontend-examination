export interface Packages {

    // utan ? får dessa inte vara null. Sätter man ? innan : kan de ha värden
    id: number;
    header: string;
    description: string;
    extendedDescription: string;
    imageUrl: string;
}
